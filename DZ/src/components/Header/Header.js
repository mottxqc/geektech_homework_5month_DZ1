import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <header
        {...this.props}
      >
        Header
      </header>
    );
  }
}

export default Header;